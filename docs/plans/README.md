# Plans

These documents are written primarily for LLM consumption. Humans do collaborate on them, but they are authored and
maintained so that an agent can pick one up and implement it with minimal extra context.

Each plan is a self-contained spec: goal, scope, design, implementation units, and testable acceptance criteria. Move a
plan into `done/` once it has been implemented.
